# src

Astro source for the marketing site: `pages/` (file-based routes), `layouts/` (shared
shells), `styles/` (global CSS). Build inputs only — the deployable artifact is
the generated `dist/` (git-ignored).
